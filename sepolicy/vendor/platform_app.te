allow platform_app hal_alarm_qti_default:binder call;
allow platform_app hal_alarm_qti_hwservice:hwservice_manager find;
# FPS
allow platform_app sysfs_fps:file { getattr open read };

allow platform_app vendor_default_prop:file { getattr  open };
allow platform_app default_prop:property_service { set };
# ll
allow poweroffalarm_app cgroup:file { { getattr open read ioctl lock map watch watch_reads } { open append write lock map } };

# HW_Service
allow platform_app default_android_hwservice:hwservice_manager { find }
